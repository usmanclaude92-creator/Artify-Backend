node_modules/
build/
dist/
coverage/

# Vercel serverless entry point — generated by `npm run build:vercel-api`
# (esbuild bundle of server/vercelHandler.ts). Never hand-edited/committed.
api/index.mjs
.DS_Store
*.log
.env*
!.env.example

# Phase 9 — local-filesystem storage provider's dev-only object store
# (OBJECT_STORAGE_PROVIDER=none). Never used in production.
.local-storage/
